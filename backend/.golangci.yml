version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
  settings:
    govet:
      enable-all: false

exclusions:
  rules:
    # Allow unhandled errors in defer/rollback
    - linters: [errcheck]
      text: "tx\\.Rollback"
